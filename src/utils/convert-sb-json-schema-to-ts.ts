const chalk = require("chalk")

export const generateTSTypedefsFromComponentsJSONSchema = (componentsJSONSchema: any[], options: any) => {
  console.log(chalk.green("✓") + " Generating TS typedefs ***");
}